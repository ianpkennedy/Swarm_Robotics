Hop count algorithm used to create the letter "N" using an array of robot generating a virtual coordinate system.

Regular hop-count N:

<img src="Regular.png" width="100"/>


Averaged hop-count N:

<img src="Smoothed.png" width="100"/>
